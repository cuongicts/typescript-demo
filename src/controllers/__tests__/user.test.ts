describe('Test A', () => {
  test('1. A success', () => {
  });

  test('2. A fail', () => {
  });
});

describe('Test B', () => {
  test('1. B success', () => {
  });

  test('2. B fail', () => {
  });
});